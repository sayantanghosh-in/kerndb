# kerndb environment variables
# copy this file to .env and fill in your values
# NEVER commit .env to git

# password for a connection named "mydb"
KERNDB_PASSWORD_MYDB=yourpassword

# add one line per saved connection
# format: KERNDB_PASSWORD_<CONNECTIONNAME>=yourpassword